export interface PlayerResponse {
  hostAddress: string;
  onlineAddress?: string;
  port: number;
  macAddress: string;
  xuid: string;
  gamertag: string;
  machineId: string;
  sessionId: string;
}
